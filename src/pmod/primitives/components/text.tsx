import { styled } from "../../styled";

export const Heading = styled('h1', { color: 'var(--text-primary)', margin: '0' });
export const Text = styled('span', { color: 'var(--text-primary)' });
export const Muted = styled('span', { color: 'var(--text-muted)', fontSize: '14px' });
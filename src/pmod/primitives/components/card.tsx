import { styled } from "../../styled";

export const Card = styled('div', {
  padding: 'var(--space-md)', background: 'var(--bg-soft)',
  borderRadius: 'var(--radius-base)', boxShadow: 'var(--shadow-soft)'
});

export const card = Card;
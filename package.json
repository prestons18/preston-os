{
  "name": "prestonos",
  "version": "1.0.0",
  "description": "My OS-style personal portfolio.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "tsc && node build.mjs",
    "build:production": "tsc && node build.mjs --prod",
    "type-check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "Preston Arnold",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "latest",
    "typescript": "latest"
  },
  "dependencies": {
    "@prestonarnold/fuse": "^0.1.0",
    "gray-matter": "^4.0.3",
    "lucide": "^0.548.0",
    "marked": "^16.4.1"
  }
}
